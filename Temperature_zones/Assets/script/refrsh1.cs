using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class refresh1 : MonoBehaviour
{
    
    // Update is called once per frame
    void Refresh()
    {
        SceneManager.LoadScene(0);
    }
}
